import sys

# 파일 개수 N
n = int(sys.stdin.readline().rstrip())

# {} -> 이건 딕셔너리임.
extension_name = {}


for _ in range(n) :
    name, extension = sys.stdin.readline().rstrip().split()
    
    if extension_name.count() == 0 :
        extension_name.append((extension, 1))
    else :
        extension_name.
    


    